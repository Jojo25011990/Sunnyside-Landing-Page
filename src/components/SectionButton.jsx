const SectionButton = ({ className = "" }) => {
	return (
		<a
			href="#"
			className={`self-baseline  text-neutral-950 ml-1.5 ${className}`}
		>
			learn more
		</a>
	);
};

export default SectionButton;
